version: '3.8'
services:
  db:
    image: postgres:15
    container_name: eco2_pg
    environment:
      POSTGRES_USER: eco2
      POSTGRES_PASSWORD: eco2password
      POSTGRES_DB: eco2
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
